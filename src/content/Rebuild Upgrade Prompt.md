# A new digital garden spec

## Overview

As a general overview, I'd like a minimal and elegant site that hosts my obsidian markdown notes, with plugins for handling the bookshelf and the movies, an elegant music player with a webgl visualiser window, webgl reactive backgrounds with vectors, dots, ascii, and an oscillating node network modes (find fast and minimal compute solution). Telescopic text for procedural text unfolding, and a graph view with d3 force layout (find fast and minimal compute solution). A login system to manage the content, and a custom markdown parser that can handle the extra features I want to add. The site should be fast and responsive, with a focus on user experience and aesthetics. 

There's also a wiki section of the site, which we should extract into its own dedicated repo, and then use a custom cname (wiki.subsurfaces.net) to point to it. This will allow us to manage the wiki content separately from the main digital garden content, and will also allow us to use a different tech stack if we want to (for example, we could use a static site generator like Hugo or Jekyll for the wiki, while using a custom React app for the main digital garden).

We're going to plan a full rebuild of the digital garden, with a focus on minimalism, elegance, and efficiency. The new version will be its own git repo (digital garden v2) and will reuse minimal code snippets from the current version while transferring what works. We'll build customisation in from the start so that I can expand the site myself easily, and we'll add a login system with careful management of keys and the database to prevent memory leaks.

A couple other things, there's a custom chess background which i like and we should find a way to integrate that into the new version, but also my photography which is featured in the site should be more easily accessible and featured more prominently. I'd appreciate any suggestions for more aesthetic things to add, one thing I like is a modern terminal aesthetic (see avatar bootup screen) which we could integrate into the design in some way - maybe as a background mode, or as a way to display the content of the notes in a more visually interesting way. We could also add some subtle animations and transitions to make the site feel more dynamic and engaging. We can replace the lichess embed on the chess page with a custom chess implementation that we build ourselves, which would allow us to have more control over the design and functionality of the chess page, and would also allow us to integrate it more seamlessly with the rest of the site - unless there is a nice customisable chess embed solution that we can use that would save us time and still look good. In terms of styling, i think the current site is a bit too busy and cluttered, so we should aim for a more minimalist and elegant design with a focus on typography and whitespace. We can use a limited color palette and simple geometric shapes to create a cohesive and visually appealing design. We should also make sure that the site is responsive and looks good on all screen sizes, from mobile to desktop. The typography is nice, though it seems a little bold and large in some places, so we could experiment with different font weights and sizes to find a better balance. We should also make sure that the typography is consistent throughout the site, and that it complements the overall design aesthetic - use postgrad understanding of web design typography and graphic design to propose a coherent stylistic vision (I will definitely be biased towards something minimal, with cute expressive ascii and terminal features here and there).

Overall, I'm looking for a site that is not only functional and efficient, but also visually stunning and enjoyable to use. I'm open to any suggestions or ideas you have for how to achieve this, and I'm excited to see what we can create together.

Ask me as many detailed questions you need until you have clarity. Bare in mind the current repo has uncommitted changes to git, and I think the new version should be its own git repo (digital garden v2 - dont use quartz naming because we're gonna be using our own custom minimal implementations for efficiency). Let's also build customisation in from the start so i can expand the site myself easily (we can add a login system (be careful to manage the keys and the db, and to have checks for memory leaks)). 
